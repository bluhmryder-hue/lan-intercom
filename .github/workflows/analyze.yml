name: "Phase 1: Analyze"
on:
  issues:
    types: [opened, edited]
  pull_request:
    types: [opened, edited]

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Analyze Issue
        run: |
          echo "Analyzing context..."
          # Logic to trigger Plan phase
